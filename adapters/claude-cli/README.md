# Shared CLI adapter

This adapter uses the same `src/cli.mjs` status command and `loadMemberWorkspace(client)` helper. It does not invoke or install a model CLI, mint execution grants, or copy credentials. Connect an authorized Platform session once real agent authentication is available; local demo usage is documented at the repository root.
